# Keep Hermes in an isolated environment. FastMCP 3 requires the newer HTTP
# stack and conflicts with NanoClaw's currently pinned FastAPI test stack.
fastmcp==3.4.5
python-dotenv==1.2.2
cryptography>=42        # Ed25519 policy-manifest signing (policy_keys.py)

# Optional CRM adapters. Production constituent workflows keep MCP writes
# disabled; install only the adapter that has been explicitly approved.
requests==2.33.0
gspread==6.2.1
Office365-REST-Python-Client==2.6.2

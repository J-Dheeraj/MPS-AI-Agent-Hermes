# Hermes vetter policy profile (offline, production-safe template).

model:
  provider: ollama
  name: llama3.1:8b
  base_url: http://127.0.0.1:11434

gateway:
  telegram:
    token: ""
    allowed_users: []
    allow_groups: false

skills:
  evolution:
    enabled: false
    auto_capture: false
  curator:
    enabled: false

mcp:
  servers: {}

memory:
  enabled: false

logging:
  level: info
  file: ~/.hermes/profiles/mps-vetters/logs/hermes.log
